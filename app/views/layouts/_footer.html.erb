
  <footer class="navbar-fixed-bottom portfolio-footer">
    <div class="container">
      <div class="row">
        <div class="col-md-6 col-sm-12">
          <p>
            <%= @settings.footer_apropos %>
          </p>
        </div>
        <div class="col-md-6 col-sm-12">
          <div class="pull-right">
            <div class="footer-links">
              <% @links.each do |link| %>
                <%= puts_footer_icon(link) %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </footer>

